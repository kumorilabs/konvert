# mysql 8.6.2

Generated by:

``` shell
helm template mysql mysql \
    --repo https://charts.bitnami.com/bitnami \
    --version 8.6.2 \
    --namespace="mysql" \
    --set "auth.username=admin,auth.password=password,auth.rootPassword=password" \
    > internal/functions/fixtures/mysql-8.6.2/fixture.yaml
```
