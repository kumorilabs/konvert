type: helm
source: https://kubernetes-charts.storage.googleapis.com
name: postgresql
values:
  global:
    postgresql:
      existingSecret: fore-postgresql
  persistence:
    enabled: true
  postgresqlDatabase: testdb
  postgresqlUsername: testuser
