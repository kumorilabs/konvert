source:
  type: helm
  config:
    name: stable/postgresql
    version: 5.3.12
    namespace:
      name: konvert-testing
      create: true
    values:
      global:
        postgresql:
          existingSecret: fore-postgresql
      persistence:
        enabled: true
      postgresqlDatabase: testdb
      postgresqlUsername: testuser
